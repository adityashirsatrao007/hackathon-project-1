## Redis Connection Fix ✓

### Problem
App crashed at startup because `REDIS_HOST` defaults to `localhost` and no Redis instance was reachable on Render. A Render KV instance (`tracelify-redis`, ID `dpg-d8ph5fm7r5hc73ajrg10`) existed but the API returned `connectionInfo: null`.

### Fix Applied (Option B: Graceful degradation)
3 files changed, +43 -21:

1. **`backend/app/core/redis.py`** — `get_redis_pool()` returns `None` instead of raising when Redis is unreachable. Added `_redis_disabled` flag to skip reconnection. `push_to_queue()` and `pop_from_queue()` no-op when pool is `None`.
2. **`backend/main.py`** — Worker only starts if Redis connects. Shutdown handles `worker_task = None`.
3. **`backend/app/services/ingest_service.py`** — Wraps `push_to_queue()` in try/except so ingest succeeds even when queue fails.

### Next Steps
- If a Redis instance becomes available, set `REDIS_HOST`, `REDIS_PORT`, `REDIS_PASSWORD`, and `REDIS_SSL=true` in Render env vars
- Consider the Upstash free tier as an alternative
